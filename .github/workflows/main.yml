name: Mi_Primer_Despliegue
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Ejecutar comando directamente en mi Debian
        run: |
          echo "¡Hola desde GitHub Actions!" > ~/hola mundo.txt
          echo "Se hizo la actualicion en mexico ." >> ~/hola mundo.txt
          date >> ~/hola mundo.txt
