name: Mi_Primer_Despliegue
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Ejecutar comando directamente en mi Debian
        run: |
          echo "¡Hola desde GitHub Actions!" > ~/hola_mundo.txt
          echo "El despliegue automático funcionó." >> ~/hola_mundo.txt
          date >> ~/hola_mundo.txt
